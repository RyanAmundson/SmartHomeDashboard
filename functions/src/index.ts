import * as functions from 'firebase-functions';

// // Start writing Firebase Functions
// // https://firebase.google.com/docs/functions/typescript
//
export const generateChoreRotation = functions.https.onRequest((request, response) => {
    //todo: add a new rotation to firebase
});

export const rotateChores = functions.https.onRequest((request, response) => {
    //todo: automatically rotate chores on sunday
});

